apiVersion: v1
kind: Service
metadata:
  name: ms-image-cache
  labels:
    app: ms-image-cache
spec:
  selector:
    app: ms-image-cache
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
